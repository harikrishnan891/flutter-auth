name: kite_authenticator
description: "Official KITE Authenticator app"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.4.1 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  google_fonts: ^6.0.0
  http: ^1.1.0
  flutter_secure_storage: ^8.0.0
  local_auth: ^2.1.6
  flutter_animate: ^4.3.0
  shared_preferences: ^2.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/logo_kite.png

